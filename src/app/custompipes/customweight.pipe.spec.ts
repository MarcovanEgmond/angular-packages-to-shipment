import { CustomweightPipe } from './customweight.pipe';

describe('customweightPipe', () => {
  it('create an instance', () => {
    const pipe = new CustomweightPipe();
    expect(pipe).toBeTruthy();
  });
});
